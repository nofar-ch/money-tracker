

  const styles = theme => ({
    inTable: {
      position: 'absolute',
      padding: '10px',
      left: 30
    },
    exTable: {
      position: 'absolute',
      padding: '10px',
      right: 30
    },
    total: {
      color: 'red',
       display: 'inline-block'
    }
});
  
  export default styles;